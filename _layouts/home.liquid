<!doctype html>
<html lang="{{ site.lang }}">
  <head>
    {% include head.liquid %}
  </head>

  <body class="portfolio-body">
    {{ content }}
    <script defer src="{{ '/assets/js/portfolio-home.js' | relative_url | bust_file_cache }}"></script>
  </body>
</html>
